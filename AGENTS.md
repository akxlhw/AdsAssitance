# CoupangAds — 面向 AI 编码代理的项目指南

> 本文档面向不了解本项目的 AI 编码代理。它基于仓库现有内容（README.md 与 `docs/` 下的规划文档）整理，不含推测。
>
> **重要：当前仓库仅有规划/设计文档，没有源代码、构建配置文件或运行时代码。** 当你开始实现时，应以本文档与 `docs/` 中的规格为准。

---

## 1. 项目概述

**CoupangAds** 是一套面向韩国 Coupang 电商平台的 **AI 内容生成系统**，目标用户是面向韩国市场的中国跨境电商卖家/运营团队。

- **核心价值**：把产品实拍图自动转化为可直接上架的韩文商品文案与详情页视觉素材。
- **当前形态**：单机命令行工具（规划中），无服务端、无数据库、无队列。
- **当前仓库状态**：仅有 `README.md` 和 `docs/*.md` 共 10 份规划文档，**没有 `pyproject.toml`、`package.json`、`Cargo.toml`、源码目录或任何构建配置**。

### 1.1 核心内容生成链路

输入层 → 文本生成引擎 → 图像生成引擎 → 输出层：

| 阶段 | 模块 | 输出 |
|------|------|------|
| 输入 | 产品实拍图文件夹（3~10 张） | — |
| 文本 | 商品画像分析 | `productreport.md` |
| 文本 | 商品标题生成 | `product_title.md` |
| 文本 | 关键词挖掘 | `wing_keywords.md` |
| 文本 | 详情页卖点文案 | `selling_points.md` + `selling_points.json`（B1-B9） |
| 文本 | 社媒文案生成 | `instagram.md` |
| 图像准备 | 智能参考图筛选 | `best_reference_images.json` |
| 图像准备 | 图片提示词组装 | `image_prompts.json` |
| 图像生成 | 详情页图片生成 | `A_B1.png` ~ `A_B9.png`、`B_B1.png` ~ `B_B9.png` |

### 1.2 目标用户

- 面向韩国市场的跨境电商卖家；
- 月 SKU 50~500 的中小型运营团队；
- 无专职韩文文案与设计师的内容团队。

---

## 2. 仓库结构

```text
D:/AI/g-pic/CoupangAds/
├── README.md                          # 项目简介、文档索引
├── AGENTS.md                          # 本文件
└── docs/
    ├── 00-商业计划书.md
    ├── 01-需求规格说明书.md
    ├── 02-系统架构设计.md
    ├── 03-功能规格说明书.md
    ├── 04-用户操作手册.md
    ├── 05-技术白皮书.md
    ├── 06-部署与运维指南.md
    ├── 07-安全与合规.md
    ├── 08-测试与验收.md
    └── 09-产品路线图.md
```

---

## 3. 技术栈与运行时架构

### 3.1 规划中的技术栈

| 层级 | 选型 | 说明 |
|------|------|------|
| 语言 | Python 3.10+ | 主要开发语言 |
| 文本模型 | Google Gemini 2.5 Flash | 主线路，多模态、韩文能力强 |
| 文本模型 | 字节 Doubao-seed-2-0-lite | 备用线路，国内访问快、成本低 |
| 图像模型 | Google Gemini 2.5 Flash Image | 图片生成主线路 |
| 图像模型 | 字节 Doubao-seedream-5-0 | 图片生成备用线路 |
| 图像处理 | Pillow + pillow-heif | 图片加载、HEIC 解码、缩放、RGB 转换 |
| DOCX 解析 | 标准库 `zipfile` + `xml.etree.ElementTree` | 不引入 `python-docx` |
| CLI | argparse | 命令行入口 |
| 测试 | pytest + pytest-cov | 规划中 |

### 3.2 运行时架构（规划）

```text
表现层 (CLI 入口)
    ├── 经典版入口（报告 + 提示词 + 图片三段式）
    ├── 完整版入口（全链路）
    └── 备用线路入口（Doubao 线路）
编排层
    ├── CLI 参数解析
    ├── 输入校验
    ├── 单产品全流程编排
    └── 缓存复用决策
业务逻辑层
    ├── 商品画像分析服务
    ├── 标题/关键词/卖点/INS 文案生成服务
    ├── 卖点解析服务（B1-B9 结构化）
    ├── 智能参考图筛选服务
    ├── 图片提示词组装服务（A/B 双风格）
    ├── 穿戴方向识别服务（服饰类专用）
    └── 断点续跑状态管理服务
适配层
    ├── GeminiClientAdapter（对话模式文本 + 独立请求图片）
    └── DoubaoClientAdapter（手动维护消息历史 + 图片生成）
基础设施层
    ├── IO 工具
    ├── 图像处理
    ├── 文件系统
    ├── 日志系统
    └── API 密钥管理
```

### 3.3 状态持久化

- **无数据库**。状态通过文件系统推断：
  - 输出文件存在 + 非强制覆盖 + 上游未变更 → 跳过；
  - 输出文件不存在 → 执行；
  - `--overwrite` → 强制重新生成；
  - 上游文件更新于当前文件 → 识别为过期（STALE）并重建。

---

## 4. 构建与运行命令

> 当前仓库没有可执行代码。以下命令来自规划文档，是后续实现的目标。

### 4.1 环境准备

```bash
# 检查 Python 版本（要求 >= 3.10）
python --version

# 创建虚拟环境
python -m venv venv

# 激活（Windows）
venv\Scripts\activate
# 激活（macOS / Linux）
source venv/bin/activate
```

### 4.2 依赖安装

```bash
# 双线路同时安装
pip install --upgrade google-genai "openai>=1.0" Pillow pillow-heif
```

### 4.3 配置 API Key

```bash
# 明文文件方式（当前规划方案）
echo "YOUR_GEMINI_API_KEY" > apikey.md
echo "YOUR_DOUBAO_API_KEY" > dbkey.md

# 必须加入 .gitignore
echo "apikey.md" >> .gitignore
echo "dbkey.md" >> .gitignore
```

### 4.4 运行命令

```bash
# 处理全部产品
python 完整版入口

# 只处理一个产品
python 完整版入口 --limit-folder "产品文件夹名"

# 只生成文字，不生成图片
python 完整版入口 --max-images 0

# 全部重新生成（覆盖已有文件）
python 完整版入口 --overwrite

# 从断点恢复（图片生成到一半中断）
python 完整版入口 --start-from A_B4

# 先检查文件结构，不调用 API
python 完整版入口 --dry-run
```

### 4.5 通用 CLI 参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--input-dir` | `raw-material` / `输入目录` | 输入目录 |
| `--output-dir` | `result` / `输出目录` | 输出目录 |
| `--api-key-file` | `apikey.md` / `dbkey.md` | API 密钥文件 |
| `--limit-folder` | — | 仅处理指定产品 |
| `--overwrite` | `false` | 强制覆盖 |
| `--dry-run` | `false` | 只扫描不调用 API |
| `--max-images` | — | 最大图片数量，`0` = 仅文本 |
| `--start-from` | — | 从指定键名恢复，如 `A_B4` |
| `--max-retries` | `3` | API 错误重试次数 |
| `--request-delay` | `2.0` | 请求间隔（秒） |
| `--request-timeout` | `300000` | API 超时（毫秒） |

---

## 5. 代码组织与模块划分

> 当前仓库尚未创建源码文件。规划中的模块划分如下：

### 5.1 入口文件

- `经典版入口`：报告 + 提示词 + 图片三段式；
- `完整版入口`：报告/标题/关键词/卖点/INS/图片全链路；
- `备用线路入口`：Doubao 模型线路。

### 5.2 业务模块

| 模块 | 职责 |
|------|------|
| 商品画像分析 | 基于实拍图输出结构化商品报告 |
| 标题/关键词/卖点/INS 生成 | 文本链路各阶段内容生成 |
| 卖点解析 | 将自由文本解析为 B1-B9 JSON |
| 参考图筛选 | 从实拍图中选出 3 张最佳参考图 |
| 图片提示词组装 | 组合 A/B 风格 + B1-B9 + 产品上下文 |
| 穿戴方向识别 | 宠物服饰类专用方向判断 |
| 断点续跑管理 | 基于文件存在性推断状态 |

### 5.3 关键输出文件命名

```text
输出目录/
└── 产品名/
    ├── productreport.md
    ├── product_title.md
    ├── wing_keywords.md
    ├── selling_points.md
    ├── selling_points.json
    ├── instagram.md
    ├── product_context.md
    ├── image_prompts.json
    ├── best_reference_images.json
    ├── A_B1.png ~ A_B9.png
    ├── B_B1.png ~ B_B9.png
    └── run.log
```

---

## 6. 开发约定

### 6.1 编码风格

- **注释以中文为主**；
- 使用 `pathlib.Path` 处理路径，保证 Windows/macOS/Linux 跨平台；
- 提示词模板外置（Markdown / DOCX），调整输出内容时**不修改代码**；
- 业务逻辑尽量写成**纯函数**，无副作用，便于测试；
- 日志格式统一：`YYYY-MM-DD HH:MM:SS | LEVEL | message`。

### 6.2 关键常量

| 常量 | 值 | 说明 |
|------|-----|------|
| 文本模型（Gemini） | `gemini-2.5-flash` | 文本生成 |
| 图像模型（Gemini） | `gemini-2.5-flash-image` | 图片生成 |
| 文本模型（Doubao） | `doubao-seed-2-0-lite-260215` | 文本生成 |
| 图像模型（Doubao） | `doubao-seedream-5-0-260128` | 图片生成 |
| 参考图长边上限 | `1536 px` | 控制传输体积 |
| 生图引用参考图数 | `3` 张 | 平衡精度与成本 |
| 详情页屏数 | `10` 屏 | 标准详情页结构 |
| 图片比例 | `2:3` | 竖屏详情页 |

### 6.3 设计原则

1. **简单可依赖**：单机即可运行，降低部署成本；
2. **模型无关**：Gemini 与 Doubao 双线路对等实现，未来可扩展；
3. **模板驱动**：生成逻辑通过外部模板驱动，运营可独立调整风格；
4. **容错优先**：单点失败不影响全局，断点续跑保障批量任务可靠。

### 6.4 容错与重试

- API 临时错误（429/500/502/503/504）：自动重试，最多 3 次，间隔 2 秒；
- 单张图片生成失败：记录错误，继续下一张；
- 单个产品处理失败：记录异常，继续下一个产品；
- 模型返回非结构化文本：多层 fallback（正则提取 → 文本兜底 → 本地组装）。

---

## 7. 测试策略

> 当前仓库没有测试代码。规划中的测试策略如下。

### 7.1 分层测试

| 层级 | 覆盖范围 | 频率 |
|------|----------|------|
| 单元测试 | 纯函数：JSON 提取、卖点分段、风格规则提取、路径处理等 | 每次代码变更 |
| 集成测试 | IO 工具 + 解析函数 + 模板加载 | 每次代码变更 |
| 端到端测试 | 完整链路：输入图 → 全部输出资产 | 发布前 / 模型升级后 |

### 7.2 可测试纯函数示例

- `extract_json_object`：代码块包裹、裸 JSON、无 JSON、嵌套 JSON；
- `parse_prompts`：字符串列表、字典列表、混合格式、空列表；
- `parse_selling_points`：标准 Markdown、编号格式、缺失部分、乱序；
- `extract_style_rules`：标准 A/B 块、单风格、无风格标记；
- `is_suspected_ai_image`：命中模式、未命中、大小写混合。

### 7.3 运行测试（规划）

```bash
# 安装 pytest
pip install pytest pytest-cov

# 运行测试
pytest tests/ -v

# 覆盖率报告
pytest tests/ --cov=. --cov-report=html
```

### 7.4 覆盖率目标

| 模块 | 目标覆盖率 |
|------|------------|
| 解析函数 | ≥ 90% |
| IO 工具 | ≥ 80% |
| 提示词组装 | ≥ 60% |
| 业务主流程 | E2E 覆盖 |

### 7.5 端到端冒烟测试 checklist

- 上传测试产品图，运行完整链路；
- 验证所有输出文件正常生成；
- 验证韩文输出无中文/英文混入；
- 验证图片为 2:3 竖屏比例；
- 验证断点续跑功能正常。

### 7.6 质量基线

| 指标 | 合格线 | 优秀线 |
|------|--------|--------|
| 商品识别准确率 | ≥ 85% | ≥ 95% |
| 韩文文案可用率 | ≥ 80% | ≥ 90% |
| 图片产品还原度 | ≥ 80% | ≥ 90% |
| 图片合规率 | 100% | 100% |
| 整体可用率 | ≥ 75% | ≥ 85% |

---

## 8. 安全与合规

### 8.1 数据分级

| 数据类型 | 分级 | 存储位置 |
|----------|------|----------|
| API Key | 🔴 机密 | `apikey.md` / `dbkey.md` |
| 产品实拍图 | 🟡 内部 | 本地输入目录 |
| 生成内容 | 🟡 内部 | 本地输出目录 |
| 提示词模板 | 🟢 公开 | 项目仓库 |
| 运行日志 | 🟡 内部 | 本地日志文件 |

### 8.2 API 密钥安全

- **当前机制**：明文单文件存储，应用读取首行非空内容；
- **必须措施**：加入 `.gitignore`，生产环境使用环境变量或 KMS；
- 生产建议使用 HashiCorp Vault / AWS Secrets Manager / 阿里云 KMS。

### 8.3 内容合规

系统输出需符合韩国广告法与 Coupang 平台规则：

- 禁用绝对化词语：如 `최고`、`완벽`、`100%`、`무조건` 等；
- 禁用医疗化表达：如 `치료`、`항균`、`예방` 等无依据功效宣称；
- 图片内禁止价格、折扣、促销信息；
- 图片文字必须为韩文，禁止中文/英文/乱码；
- 禁止虚构产品未具备的功能或属性。

### 8.4 免责声明

> 系统生成的所有内容仅供参考，不构成法律建议或合规保证。最终上线内容必须经人工审核，由使用者承担全部商业和法律责任。

### 8.5 应急响应

| 场景 | 关键操作 |
|------|----------|
| API Key 泄露 | 5 分钟内在模型平台撤销/轮换 Key；30 分钟内检查异常用量；1 小时内更新配置并验证 |
| 生成违规内容 | 立即下线/修正；批量问题暂停该批次并检查提示词模板 |

---

## 9. 部署与运维

### 9.1 推荐部署形态

- **单机 CLI**：当前规划形态；
- **服务器部署**：Docker / systemd / supervisor + Cron 调度；
- **未来演进**：v2.0 规划 Web 平台（FastAPI/Flask + React/Vue + Celery/Redis + PostgreSQL + S3/MinIO）。

### 9.2 运维命令

```bash
# 实时查看日志
tail -f "输出目录/{产品名}/run.log"

# 查看错误日志
grep "ERROR" 输出目录/*/run.log

# 统计成功/失败数量
grep -r "Saved image:" 输出目录/ | wc -l
grep -r "Failed to generate" 输出目录/ | wc -l

# 检查输出目录大小
du -sh 输出目录/
```

### 9.3 输入输出目录约定

- 默认输入目录：`raw-material` / `输入目录`；
- 默认输出目录：`result` / `输出目录`；
- 每个产品一个独立文件夹，文件夹名为产品名；
- 产品实拍图建议 3~10 张，格式 JPG/PNG/HEIC/WEBP。

---

## 10. 产品路线图

| 版本 | 时间 | 重点 |
|------|------|------|
| v1.0 | 2025-06 | 当前：基础链路、双模型、断点续跑、模板外置 |
| v1.5 | 2025-Q3 | 质量审核层、广告法过滤、批量优化、补充单元测试 |
| v2.0 | 2025-Q4 ~ 2026-Q1 | Web 平台、任务队列、多租户、对象存储 |
| v3.0 | 2026 年以后 | Agent 决策、自然语言运营、数据反馈闭环 |

### 10.1 当前已知技术债务

- 无单元测试覆盖；
- 无 Web 界面，纯命令行操作；
- API Key 明文存储；
- 单线程串行处理。

---

## 11. 给 AI 编码代理的实操提示

1. **当前无代码**：本仓库目前只有文档。任何实现工作都需要从零创建源码、配置文件和测试。
2. **以文档为唯一权威**：功能、架构、CLI 参数、输出文件命名、合规规则均在 `docs/` 中详细定义。
3. **优先实现 `完整版入口` 全链路**：商品画像 → 标题 → 关键词 → 卖点 → INS → 图片生成。
4. **保持双线路对等**：Gemini 与 Doubao 的输出文件结构和命名必须完全一致。
5. **模板外置**：所有 Prompt 通过 Markdown / DOCX 文件驱动，不要硬编码到代码中。
6. **中文注释**：代码注释以中文为主，日志格式统一为 `YYYY-MM-DD HH:MM:SS | LEVEL | message`。
7. **路径安全**：使用 `pathlib.Path`，避免路径遍历；不要执行用户输入作为命令。
8. **API Key 文件**：实现读取 `apikey.md`/`dbkey.md` 首行非空内容；同时支持环境变量 `GEMINI_API_KEY` / `DOUBAO_API_KEY`。
9. **测试先写**：优先为解析函数和 IO 工具补充 pytest 单元测试，再覆盖主流程 E2E。
10. **.gitignore**：务必把 `apikey.md`、`dbkey.md`、虚拟环境、输出目录、日志等敏感/生成文件加入 `.gitignore`。
